/**
 * Functions as the driver for the cryptogram project
 * This creates a new FirstFrame to allow the user to select start up choice.
 */
public class CryptoGui {
    public CryptoGui() {
        playerFrame first = new playerFrame();
    }

    public static void main(String[] args) {
        CryptoGui gui = new CryptoGui();
    }
}
